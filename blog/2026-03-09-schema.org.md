---
title: Is it http://schema.org or https://schema.org?
authors: [coret, ddeboer]
tags: [rdf]
---

Schema.org is the web’s most widely adopted vocabulary for structured data, supported by Google, Bing, and countless data platforms.
It defines types like `CreativeWork`, `Person`, and `Organization`, giving machines a common language to understand content.
This is exactly why the [NDE application profile](https://docs.nde.nl/schema-profile/) is built on Schema.org.
Getting it right is what makes cultural heritage data findable and reusable across institutions.

But if you’ve worked with Schema.org in RDF, you’ve run into this: there’s `http://schema.org` and then there’s `https://schema.org`.
That one-letter difference can cause real problems:
SPARQL queries that silently return nothing, SHACL validation that rejects good data or ignores bad data, or datasets that should link up but don’t – especially when combining data from multiple sources, as NDE does.

<!--truncate-->

## How did this happen?

Historically, Schema.org identifiers were defined using `http://schema.org/`.
In 2018, as part of the [broader web push towards HTTPS](https://github.com/schemaorg/schemaorg/issues/1925), the *website* moved to `https://`. Schema.org’s own [FAQ](https://schema.org/docs/faq.html) had already declared ([since 2015](https://github.com/schemaorg/schemaorg/commit/590bd1a071edc8edcf3d71a9ea110aa46eeb4edc)) that “both ‘https://schema.org’ and ‘http://schema.org’ are fine.” So people naturally started using `https://` – copying it from the site, its examples, and following the FAQ advice – even though the vocabulary identifiers were still defined as `http://`.

But a webpage is not the same as an identifier. Your browser just “ends up” at `https://schema.org/` either way, but for any tool that processes RDF, `http://schema.org/CreativeWork` and `https://schema.org/CreativeWork` are **two different things**.

As Tim Berners-Lee wrote in [*Axioms of Web Architecture*](https://www.w3.org/DesignIssues/Axioms.html): “the significance of identity for a given URI is determined by the person who owns the URI, who first determined what it points to.” So what did Schema.org’s owner determine?

## Why “both are fine” is misleading

Schema.org’s owner is sending two contradictory signals: the FAQ says both are fine, but the JSON-LD context defines the vocabulary with `http://` identifiers. The website examples use `https://schema.org/`, while the JSON-LD context uses `http://schema.org/`. The FAQ advice works for search-engine consumers like Google that normalize both variants, but for JSON-LD processors and RDF tooling, they are *not* the same.

## JSON-LD contexts resolve to `http://`

JSON-LD is a common RDF serialization format. Schema.org uses it in its website examples.
When you write Schema.org in JSON-LD, it looks like this:

```json
{
  "@context": "https://schema.org",
  "@type": "CreativeWork"
}
```

JSON-LD processors follow a standardized mechanism called [remote document and context retrieval](https://www.w3.org/TR/json-ld11-api/#remote-document-and-context-retrieval):

1. The processor sees `@context: "https://schema.org"`.
2. It fetches that URL. The server responds with a `Link` header pointing to the JSON-LD context: `</docs/jsonldcontext.jsonld>; rel="alternate"; type="application/ld+json"`.
3. The processor follows that link to `https://schema.org/docs/jsonldcontext.jsonld`.

Inside that document you’ll find something important:

```json
{
  "@context": {
    "@vocab": "http://schema.org/"
  }
}
```

This tells the JSON-LD processor: all Schema.org terms expand to `http://schema.org/` URIs. So even if your markup uses `https://schema.org` as the context URL, a processor expands `"@type": "CreativeWork"` to `http://schema.org/CreativeWork`.

You can see this in action on the command line:
```
echo '{"@context":"https://schema.org","@type":"CreativeWork","name":"Example"}' | riot --syntax=jsonld --output=nquads
_:B... <http://www.w3.org/1999/02/22-rdf-syntax-ns#type> <http://schema.org/CreativeWork> .
_:B... <http://schema.org/name> "Example" .
```

Tools – including structured-data validators, knowledge-graph pipelines, and libraries like Riot – normalize everything to `http://` IRIs. They aren’t downgrading security; they’re following the vocabulary definition from the official context.

## What does the Schema.org community say?

The Schema.org community has been discussing this for years without resolution:

- [**Issue #4404**](https://github.com/schemaorg/schemaorg/issues/4404) (still open) is the most recent discussion. 
  Contributors point out that “identifiers in the schema are technically URIs, not URLs. It’s actually better if they don’t change over time and there is no security implication involved in a predicate or type,” and that “Stable URIs that do not change over time are a MUST in the world of Linked Open Data.” Others describe the current situation as an “ugly intermediate zone” – neither fully `http://` nor fully `https://`.
- [**Issue #1325**](https://github.com/schemaorg/schemaorg/issues/1325) (closed, not planned) confirmed there would be no mandatory migration to `https://`.
- [**Issue #2597**](https://github.com/schemaorg/schemaorg/issues/2597) led to examples being updated to `https://`, while the context was intentionally kept at `http://`.

## Cool URIs don’t change

Tim Berners-Lee wrote in his essay [*Cool URIs don’t change*](https://www.w3.org/Provider/Style/URI): URIs should be stable over time. Once `http://schema.org/CreativeWork` became the identifier for that concept, changing it to `https://schema.org/CreativeWork` would break existing data, queries, and validation rules.

He later reflected on the HTTP-to-HTTPS transition in his book [*This is for Everyone*](https://thisisforeveryone.timbl.com/) (p. 176):

> “We hit an unfortunate snag when we changed the URL to add an ‘S’: it broke all existing links on the web! In retrospect, the web would be much more functional and simpler if HTTP and HTTPS pages both used the same URL, just with different protocols.”

The scheme change should not have affected identity – but it did.

## What does NDE recommend?

The [NDE Schema Application Profile](https://docs.nde.nl/schema-profile/) provides clear guidance:

- Publishers **SHOULD** use `http://schema.org/`, because that’s what the JSON-LD context resolves to and what aligns with the canonical vocabulary.
- Consumers **MUST** accept both `http://` and `https://` variants. A large amount of Schema.org data already exists in the wild using `https://`, and refusing it would mean ignoring perfectly valid datasets.

The rationale is documented in [schema-profile#45](https://github.com/netwerk-digitaal-erfgoed/schema-profile/issues/45). This pragmatic approach follows the [robustness principle](https://en.wikipedia.org/wiki/Robustness_principle): be strict in what you publish, liberal in what you accept.

Using `http://` for vocabulary identifiers is not unusual: Dublin Core Terms, for example, uses `http://purl.org/dc/terms/` and has never changed to `https://`.

## Conclusion

The `http://` in `http://schema.org/CreativeWork` doesn’t mean “go fetch this page over insecure HTTP.” It’s an opaque string that identifies a concept – like an ISBN identifies a book regardless of where you bought it. In RDF, URIs serve as identifiers, not as web addresses to visit – and the linked data ecosystem needs them to be stable.

When publishing structured data: use `http://schema.org/`. When consuming it: accept both.
